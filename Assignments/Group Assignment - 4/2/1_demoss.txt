SELECT TDOC_ID,WordFreq/DocSize AS PhraseScore FROM
(SELECT DOC_ID AS TDOC_ID, sum(FREQ) AS DocSize FROM INVINDEX GROUP BY TDOC_ID) 
INNER JOIN
(SELECT DOC_ID AS SDOC_ID, sum(FREQ) AS WordFreq FROM INVINDEX WHERE WORD="Hello" OR WORD="World" GROUP BY SDOC_ID) 
ON TDOC_ID=SDOC_ID;
